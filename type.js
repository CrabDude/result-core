/**
 * if your inheriting from this class purely
 * for the semantic "is-a" relationship then
 * you can just require this file directly 
 * intead of ./index.js
 *
 *   var Result = require('result-core/type')
 *   inherit(MyResultClass, Result)
 */

module.exports = function Result(){}